---
name: Quick Test

on:
  push:
    branches:
      - main

jobs:
  quick-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Read Envs
        run: printenv
